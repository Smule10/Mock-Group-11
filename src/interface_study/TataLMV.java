package interface_study;


public interface TataLMV extends Tata 
// In this we use Extends instead of Implements because , if we use Implements then we must
// have to complete that incomplete methods in IC .
{
	void car();
	void minibus();
	
}
